# Local dev — OSRM Docker on Mac; geocode via Pelias on production server (no local Pelias).
# Requires server Pelias :4000 reachable (allow-dev-ip or VPN). Tunnel: bun run dev:remote:tunnel

PORT=8080
OSRM_URL=http://localhost:5050
OSRM_MOTOR_URL=http://localhost:5051
PELIAS_URL=http://149.28.134.50:4000
GEOCODE_USER_AGENT=routing-api-dev/1.0 (dev@localhost)
ROUTING_DURATION_FACTOR_CAR=1.25
ROUTING_DURATION_FACTOR_MOTORBIKE=0.85
