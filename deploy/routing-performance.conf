# /etc/nginx/conf.d/routing-performance.conf — upstream + rate limits

limit_req_zone $binary_remote_addr zone=routing_api:10m rate=200r/s;
limit_req_zone $binary_remote_addr zone=routing_geocode:10m rate=40r/s;

upstream routing_api {
    server 127.0.0.1:8080;
    keepalive 128;
}
